#ifndef JIROEDITER_H
#define JIROEDITER_H

#include <QMainWindow>

class JiroEditer : public QMainWindow
{
    Q_OBJECT
public:
    explicit JiroEditer(QWidget *parent = 0);

signals:

public slots:

protected:

    int windowHeight;
    int windowWidth;

};

#endif // JIROEDITER_H
